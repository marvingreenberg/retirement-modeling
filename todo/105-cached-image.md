105-Cached-image

Whenever any inputs are changed, the accounts, expenses, budget, new data is loaded,
the cached image and state from the UI are cleared (back to "Ready to simulate")

The Done button on save load is removed, Instead a  to a "Left Navigation label" like
<= <Icon> Overview.  Redundant with just clicking the <icon> Overview button, but that is OK

The load sample data is moved from Basic Info.  Instead it is Moved to
Load/Save and the startup message also says "Or Use Load/Save to load previously saved data, or sample data"
Having the Load, then below Load Sample Data with that pull down.
