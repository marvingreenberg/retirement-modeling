multiselect-for-qfx load

Allow for the +Account to multiselect the QFX files to load, and somehow manage the multiple load

It may need to load them all and then iterate for the user to enter name/type/etc
(Which maybe already happens for a multiaccount QFX??)
Or else same behavior as multiaccount -- create them all, user must update as appropriate

Can the Chromium select also limit the files prosented like only show *.OFX, *.QFX?

And a DEMO capability, add to the UI a way to save the system state with the totals modified
by random ratio, 0.2 to 0.5, generate random name(s) for owners.  Startup flag on the UI, somehow?
Advanced setting?  or advanced setting to just randomize the account totals (run multiple time
if desired.)
