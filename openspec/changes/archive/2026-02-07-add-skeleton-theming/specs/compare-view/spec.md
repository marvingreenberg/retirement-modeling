## MODIFIED Requirements

### Requirement: Themed comparison controls and results
The Compare view strategy checkboxes, buttons, comparison table, and best-value highlighting SHALL use Skeleton/Tailwind styling consistent with the active theme and mode.

#### Scenario: Strategy checkboxes styled
- **WHEN** the Compare view renders strategy selection
- **THEN** checkboxes use Skeleton/Tailwind form styling

#### Scenario: Comparison table styled
- **WHEN** comparison results render in a table
- **THEN** the table uses Skeleton table classes with themed best-value highlighting
